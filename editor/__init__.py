"""Interactive track and car creator for SheetCircuit.

A standalone, text-mode editor (rich-only) that surfaces every knob in the
car and track schemas and writes JSON the game loader can read directly.
"""
